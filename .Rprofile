if (file.exists("~/.Rprofile")) {
  base::sys.source("~/.Rprofile", envir = environment())
}

# Blogdown global options
options(blogdown.author = "If you can read this you forgot to enter your name")
